#pragma once

#include "mx.h"

enum mx::EPlatform
{
	MX_PLATFORM_UNKNOWN, // PLATFORM_UNKNOWN is already used somewhere else.

	PLATFORM_PC,
	PLATFORM_PLAY_STATION,
	PLATFORM_XBOX,
	PLATFORM_GAME_CUBE,
	PLATFORM_WII
};